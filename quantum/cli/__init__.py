# The quantum command lives in runner.py (quantum = quantum.cli.runner:main).
# Nothing is imported here: `python -m quantum.cli.runner` warned that runner
# was already imported by the package before it ran.
