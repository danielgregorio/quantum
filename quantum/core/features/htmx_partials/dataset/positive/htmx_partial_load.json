{
  "example_id": "htmx_001",
  "category": "htmx_partials",
  "tags": ["htmx", "partial", "ajax"],
  "input": {
    "component": "<q:component name=\"LiveStats\">\n  <div q:partial=\"live_stats\" q:trigger=\"load\" q:swap=\"innerHTML\">\n    Loading...\n  </div>\n</q:component>",
    "context": {
      "phase": "B",
      "feature": "htmx_partials",
      "trigger": "load",
      "method": "GET"
    }
  },
  "expected_behavior": "On page load, renders the 'live_stats' partial and replaces div content with partial response",
  "validation": {
    "partial_loaded": true,
    "ajax_request_sent": true,
    "content_replaced": true,
    "no_full_page_reload": true
  }
}
