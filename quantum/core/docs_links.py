"""Documentation pages that error messages point to (one place, one spelling)."""

DOCS = 'https://quantumframework.net'

# EXEC-*: the order in which a page's parts run. The traps — a statement in
# the markup, a variable the action cannot see, a guard reading the page —
# all come from that order, so their messages send the reader here.
HOW_A_PAGE_RUNS = f'{DOCS}/guide/how-a-page-runs'
