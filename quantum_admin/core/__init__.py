"""The admin's library: its database models, CRUD and the services the `.q`
screens call (quantum_admin.services). Shipped with the [admin] extra.

The old FastAPI backend (quantum_admin/backend) stays in the repository but
not in the wheel; it imports these modules through shims under their old names.
"""
